# إعداد قاعدة البيانات - Database Setup

## الخطوات المطلوبة - Required Steps

### 1. تنفيذ السكيما في Supabase
يجب تنفيذ الأوامر SQL التالية في Supabase SQL Editor:

1. افتح Supabase Dashboard
2. اذهب إلى SQL Editor
3. قم بتنفيذ ملف `supabase-complete-schema.sql`

أو يمكنك نسخ المحتوى من الملف وتنفيذه مباشرة.

### 2. الجداول التي سيتم إنشاؤها

#### `canned_responses` - الردود السريعة
- تخزين الردود الجاهزة مع الاختصارات
- يستخدم في صفحة `/responses`

#### `campaigns` - الحملات الإعلانية
- تخزين الحملات الجماعية
- يستخدم في صفحة `/campaigns`

#### `campaign_recipients` - متابعة المستلمين
- تتبع حالة إرسال كل رسالة في الحملة
- مرتبط بـ `campaigns` و `chats`

#### `agent_stats` - إحصائيات الموظفين
- تخزين أداء الموظفين اليومي
- يستخدم في صفحات `/team` و `/reports`

#### `chat_tags` - التصنيفات
- تصنيفات المحادثات (شكوى، استفسار، إلخ)
- يستخدم في `/reports` و `/chat`

#### `chat_tag_relations` - علاقة المحادثات بالتصنيفات
- جدول ربط Many-to-Many

### 3. التحديثات على الصفحات

تم تحديث الصفحات التالية لاستخدام بيانات حقيقية من Supabase:

#### `/responses` - الردود السريعة
- ✅ تحميل الردود من `canned_responses`
- ✅ إنشاء رد جديد
- ✅ حذف رد (soft delete)
- ✅ نسخ محتوى الرد

#### `/campaigns` - الحملات
- ✅ تحميل الحملات من `campaigns`
- ✅ إنشاء حملة جديدة
- ✅ جدولة الحملات
- ✅ عرض الإحصائيات

#### `/reports` - التقارير
- ✅ تحميل إحصائيات المحادثات
- ✅ تحميل إحصائيات الرسائل
- ✅ تحميل التصنيفات الأكثر استخداماً
- 🔄 TODO: حساب متوسط وقت الرد
- 🔄 TODO: حساب وقت الذروة

#### `/team` - الفريق
- ✅ عرض بيانات المستخدم الحالي
- ✅ عرض الإحصائيات من `agent_stats`
- 🔄 TODO: دعوة أعضاء جدد للفريق

### 4. الميزات القادمة

- [ ] إرسال الحملات فعلياً من Worker
- [ ] حساب متوسط وقت الرد الحقيقي
- [ ] تحديد وقت الذروة من timestamps الرسائل
- [ ] نظام الأدوار (Admin/Agent) الكامل
- [ ] دعوة أعضاء الفريق عبر البريد
- [ ] استخدام الردود السريعة في الدردشة

### 5. ملاحظات هامة

- تم تعطيل Row Level Security (RLS) مؤقتاً للتبسيط
- يجب تفعيل RLS وإضافة السياسات الأمنية قبل الإنتاج
- بعض الإحصائيات لا تزال ثابتة (TODO)

---

## Execute SQL Schema

Run the content of `supabase-complete-schema.sql` in your Supabase SQL Editor to create all required tables.

All pages now use real Supabase data instead of mock data!
